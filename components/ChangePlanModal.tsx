import React from 'react';

// This component is deprecated and no longer used. 
// The pricing plan selection has been moved directly to the BillingPage.
const ChangePlanModal: React.FC = () => {
    return null;
};

export default ChangePlanModal;